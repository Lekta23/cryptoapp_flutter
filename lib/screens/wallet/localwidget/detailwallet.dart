import 'package:flutter/material.dart';

class DetailWallet extends StatelessWidget {
  const DetailWallet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
